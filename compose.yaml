services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-powered-tool:latest
    ports:
      - "5000:5000"
    environment:
      NODE_ENV: production
      PORT: 5000
    restart: unless-stopped
